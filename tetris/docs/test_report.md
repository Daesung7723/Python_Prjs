# 테트리스 게임 - 테스트 결과 보고서

## 개요

| 항목 | 내용 |
|------|------|
| 실행일 | 2026-05-18 |
| Python 버전 | 3.10.12 |
| 테스트 프레임워크 | pytest 9.0.3 |
| 실행 결과 | **82 passed** / 0 failed / 0 error |
| 총 실행 시간 | 0.46s |

---

## 테스트 파일 구성

| 파일 | 테스트 클래스 수 | 테스트 케이스 수 | 대상 모듈 |
|------|----------------|----------------|----------|
| `tests/test_board.py` | 7 | 27 | `board.py` |
| `tests/test_tetromino.py` | 3 | 19 | `tetromino.py` |
| `tests/test_score.py` | 5 | 23 | `score.py` |
| `tests/test_integration.py` | 5 | 13 | board + tetromino + score 연동 |
| **합계** | **20** | **82** | — |

---

## 모듈별 테스트 상세

### test_board.py — Board 클래스 (27 케이스)

| 테스트 클래스 | 케이스 | 검증 내용 |
|-------------|--------|---------|
| `TestBoardInit` | 3 | 보드 크기(10×20), 초기 빈 상태, 범위 외 접근 반환값 |
| `TestIsValid` | 8 | 좌·우·하단 벽 충돌, 고정 블록 충돌, 보드 위(y<0) 허용, 이동 가능 방향 |
| `TestLock` | 2 | 격자에 블록 타입 값 기록, 음수 y 좌표 무시 |
| `TestClearLines` | 7 | 빈 줄 유지, 1줄/4줄 삭제, 삭제 후 상단 빈 줄 삽입, 높이 불변, 위 블록 낙하, 미완성 줄 유지 |
| `TestGhostY` | 3 | 바닥 착지, 고정 블록 위 착지, 이미 바닥에 있을 때 |
| `TestGameOver` | 3 | 빈 보드 정상, 최상단 행 점유 시 게임 오버, 2번째 행은 정상 |
| `TestReset` | 1 | 리셋 후 격자 초기화 |

**결과**: 27 / 27 PASSED ✓

---

### test_tetromino.py — Tetromino 클래스 (19 케이스)

| 테스트 클래스 | 케이스 | 검증 내용 |
|-------------|--------|---------|
| `TestTetrominoShape` | 7 | 7종 블록 각 4회전 상태, 4×4 행렬 크기, 색상 RGB 범위, I·O 블록 형태, 블록당 정확히 4칸 (parametrize 7종) |
| `TestTetrominoInstance` | 9 | 기본 생성 위치(3,0), 4칸 좌표 반환, 위치 오프셋, CW·CCW 회전, 4회 회전 후 원상복귀, copy 독립성 |
| `TestTetrominoFactory` | 3 | create 타입 정확성, random 반환 타입 |

**결과**: 19 / 19 PASSED ✓

---

### test_score.py — ScoreManager 클래스 (23 케이스)

| 테스트 클래스 | 케이스 | 검증 내용 |
|-------------|--------|---------|
| `TestInitialState` | 3 | 초기 점수 0, 레벨 1, 줄 수 0 |
| `TestLineClear` | 8 | 1·2·3·4줄 점수(100·300·500·800), 레벨 배수, 줄 수 누적, 0줄 변화 없음 |
| `TestLevelProgression` | 4 | 10줄마다 레벨업, 테트리스 조합으로 레벨업, 최대 레벨 15 클램프, 레벨업 후 점수 배수 반영 |
| `TestDropScore` | 3 | 소프트 드롭 1점/칸, 하드 드롭 2점/칸, 기본값 1칸 |
| `TestFallInterval` | 4 | 레벨 1 = 1.000s, 레벨 증가 시 단축, 레벨 15 최솟값(0.020s), 레벨 15 초과 동일 |
| `TestReset` | 1 | 리셋 후 점수·레벨·줄 초기화 |

**결과**: 23 / 23 PASSED ✓

---

### test_integration.py — 모듈 연동 (13 케이스)

| 테스트 클래스 | 케이스 | 검증 내용 |
|-------------|--------|---------|
| `TestDropAndLock` | 2 | 블록 바닥 착지 위치, 블록 스택 쌓임 |
| `TestLineClearIntegration` | 3 | 1줄 소거 점수 연동, 4줄 동시 소거(테트리스), 소거 후 보드 높이 불변 |
| `TestHardDropScoring` | 1 | 하드 드롭 거리 × 2 점수 |
| `TestGameOverCondition` | 2 | 생성 위치 차단 시 게임 오버 판별, 정상 생성 |
| `TestRotationAtWall` | 2 | 벽 인접 회전 불가 시 원상 복귀, 7종 블록 모든 회전이 범위 내 보장 |

**결과**: 13 / 13 PASSED ✓

---

## 미커버 영역

| 모듈 | 미테스트 항목 | 비고 |
|------|-------------|------|
| `renderer.py` | 전체 | pygame Surface 의존, 수동 플레이로 검증 |
| `input_handler.py` | 전체 | pygame 이벤트 의존, 수동 플레이로 검증 |
| `game.py` | 전체 | pygame 루프 의존, 수동 플레이로 검증 |
| `game_state.py` | — | Enum 단순 정의, 별도 테스트 불필요 |

렌더링·입력 레이어는 pygame 런타임 없이 단위 테스트가 어려우며, 수동 플레이 테스트로 대체하였다.

---

## 수동 플레이 테스트 결과

| 항목 | 결과 |
|------|------|
| 방향키 블록 이동 | 정상 |
| ↑ 블록 회전 | 정상 (벽/블록 충돌 시 회전 취소 확인) |
| Space 하드 드롭 | 정상 |
| ↓ 소프트 드롭 | 정상 |
| P 일시정지 / 재개 | 정상 |
| R 재시작 | 정상 |
| 줄 삭제 및 블록 낙하 | 정상 |
| 레벨업 및 속도 증가 | 정상 |
| 게임 오버 판정 | 정상 |
| 고스트 피스 표시 | 정상 |
| 다음 블록 미리보기 | 정상 |

---

## 결론

전체 82개 단위·통합 테스트가 모두 통과하였으며, 수동 플레이 테스트에서도 요구사항(FR-01 ~ FR-33) 전 항목이 정상 동작함을 확인하였다.
